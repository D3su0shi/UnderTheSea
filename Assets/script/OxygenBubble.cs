using UnityEngine;

public class OxygenBubble : MonoBehaviour
{
    private float dockingZoneRadius; //circular radius the ship must enter to collect oxygen
    private float refillTimeDuration; //time taken to refill oxygen
    private bool isRefilling = false; //flag to check if refilling is in progress
    //public GameUI gameUI; //reference to game UI
    public subScript ship; //reference to the ship
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
